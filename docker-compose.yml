version: "3.7"
services:
    visible-consonants:
       image: "${APPNAME}:${VERSION}"
       deploy:
          replicas: 1
          resources:
             limits:
                memory: "3GB"
       ports:
          - "10008:3838"
       networks:
          - ${APPNAME}
networks:
    visible-consonants:
